
version: 2

models:
    - name: j_customers
      description: "Customer Model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: j_orders
      description: "Orders Model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
